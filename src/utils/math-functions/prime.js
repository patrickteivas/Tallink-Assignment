import getBiggestPrimeBetween from "../getBIggestPrimeBetween";

export default function prime(firstOperator, secondOperator) {
  return getBiggestPrimeBetween(Number(firstOperator), Number(secondOperator));
}
